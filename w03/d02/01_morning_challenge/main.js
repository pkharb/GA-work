function isAnagram(a,b) {
var str1; 
var str2; 
};

isAnagram();

//- check if every alphabet in str1 is also in str2
//- once we check an alphabet in both strings, delete that alphabet
// - if the remainging strings are both null, return true, otherwise return false 